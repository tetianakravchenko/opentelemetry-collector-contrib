azurefunctions:
  http:
    endpoint: test:123
  auth: azureauth
  logs:
    encoding: azure_encoding

azurefunctions/no_auth:
  http:
    endpoint: test:123
  logs:
    encoding: azure_encoding

azurefunctions/no_http:
  auth: azureauth
  logs:
    encoding: azure_encoding

azurefunctions/missing_logs_encoding:
  auth: azureauth
  http:
    endpoint: test:123